// Deprecated: the old friends drawer was removed during the production-readiness cleanup.
// Keeping this no-op export prevents broken imports while avoiding dead commented code.
const FriendsSidebar = () => null;

export default FriendsSidebar;
